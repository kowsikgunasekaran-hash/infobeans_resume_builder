[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"
  # Don't fail the build on lint warnings
  CI = "false"

# Single-page app fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
